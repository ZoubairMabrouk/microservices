version: "3.9"

services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    image: zoubairmabrouk/chat-service:${IMAGE_TAG:-latest}
    environment:
      - OL_BASE_URL=http://51.68.44.166:11434/api/chat
    ports:
      - "8080:8080"
    
  